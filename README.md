# React Native + Plop.js

Generated with `@karlmarxlopez/expo-ts-and-jest` template.

# Running the project

1. `yarn expo prebuild --clean`
1. `yarn ios -d <device name or ID>` or `yarn android`

# Usage

`yarn g` to start generating code
